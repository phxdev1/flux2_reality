name: fast
version: "1.0"
description: "Fast generation mode using LCM-style reduced steps"
category: speed

triggers:
  - fast
  - quick
  - rapid
  - speed
  - draft

params:
  num_steps_override: 8
  guidance_scale: 0.8

inject:
  modulation: false
